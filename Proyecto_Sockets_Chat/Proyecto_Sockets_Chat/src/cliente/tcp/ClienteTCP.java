package cliente.tcp;

public  class ClienteTCP{
    /*// reciba el re
    protected final String SERVER;
    protected final int PUERTO_SERVER;

    //una para que reciba el servidor al que me quiero conectar como cadena
    // y el puerto del servidor al que me quiero conectar.
    public ClienteTCP(String servidor,int puertoS){
        SERVER=servidor;
        PUERTO_SERVER=puertoS;
    }

    //metodo inicia
    public void inicia()throws Exception{
        ClienteEnviaTCP clienteTCP= new ClienteEnviaTCP(SERVER,PUERTO_SERVER);

        //hilo para que despues ejecutemos su metodo start, para que ese hilo inicie.
        clienteTCP.start();
    }*/
}
